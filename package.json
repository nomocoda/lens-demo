{
  "name": "lens-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "eval": "env -u ANTHROPIC_API_KEY node --env-file=.env evals/reviewer.mjs"
  }
}
